/**
 * @readonly
 * @enum {string}
 */
enum SIZE {
  S = 's',
  M = 'm',
  L = 'l',
}

/**
 * @readonly
 * @enum {string}
 */
enum THEME {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
}

export { SIZE, THEME };
